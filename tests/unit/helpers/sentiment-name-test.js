import { sentimentColor } from 'nuvi-challenge/helpers/sentiment-color';
import { module, test } from 'qunit';

module('Unit | Helper | sentiment color');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = sentimentColor([42]);
  assert.ok(result);
});
