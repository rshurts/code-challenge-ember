import { sentimentPanel } from 'nuvi-challenge/helpers/sentiment-panel';
import { module, test } from 'qunit';

module('Unit | Helper | sentiment panel');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = sentimentPanel([42]);
  assert.ok(result);
});
